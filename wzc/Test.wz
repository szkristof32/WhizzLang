// The entry point of the program
fn main() : int
{
    // val = 24
    const val = 6 * (8 + 8) / 4;
    if (val - 24)
    {
        const val2 = val - 10;
        return val2 + val - 3;
    }
    else if (0)
    {
        /* comment * with star */
        return 42;
    }
    val = val + 2;

    const i = 10;
    while (i)
    {
        val = val + 1;
        i = i - 1;

        if (i - 3)
        {
        }
        else
        {
            return i;
        }
    }

    return val;
}
